#include <stdio.h>  // Inclui a biblioteca padrão de entrada e saída em C

int main() {
   printf("Caderno de poesias é um belo lugar.\n");  // Imprime a primeira linha do poema na tela
   printf("Tantas coisas lindas que eu gostaria de falar.\n");  // Imprime a segunda linha do poema na tela
   printf("Eu falo em forma de versos para todos poderem escutar.\n");  // Imprime a terceira linha do poema na tela
   printf("Agora você já sabe por que os poetas passam os dias escrevendo em seus\n");  // Imprime a quarta linha do poema na tela, usando \n para quebrar a linha
   printf("cadernos de poesias.\n");  // Imprime a última linha do poema na tela
   return 0;  // Retorna o valor 0 para indicar que o programa foi executado com sucesso
}
